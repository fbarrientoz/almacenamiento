<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uabc.edu.almacenamiento">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AlmacenamientoSP_2"></activity>
        <activity android:name=".AlmacenamientoSP" />
        <activity android:name=".AlmacenamientoSharedP2" />
        <activity android:name=".AlmacenamientoSharedP" />
        <activity android:name=".AlmacenamientoSD" />
        <activity android:name=".AlmacenamientoLocal" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>